const { Display_data } = require("@/app/componets/display")

export const metadata = {
    title: 'TO FOR ZERO || DeshBoard',
}

export default function page() {
    return (
        <>
            <Display_data />
        </>
    )
}